# Design documents

Proposals and explorations that sit above the formal specification: they
consume the protocol as specified in [`spec/`](../spec) and argue for how to
build on it (or when to extend it).

| Document | Status | Companion artifacts |
|---|---|---|
| [durable-objects.md](durable-objects.md) | proposal + runnable prototypes | `resonate-sdk-ts` branch `claude/resonate-durable-objects-donm81`: `src/objects/`, `tests/objects/` |
